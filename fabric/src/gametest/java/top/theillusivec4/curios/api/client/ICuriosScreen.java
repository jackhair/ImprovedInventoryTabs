package top.theillusivec4.curios.api.client;

/**
 * Stand-in for Curios' marker interface of the same name. The mod names it
 * (by name only, it never links against Curios) to keep the left tab column
 * off Curios' slot panel, so a test screen implementing this one exercises
 * that path without Curios installed.
 */
public interface ICuriosScreen {
}
